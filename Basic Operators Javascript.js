var health = 50;
health = health + 10; // store 60 
health = health % 50 ; // store 10

// heatlh % 2 == 0
health = health ** 2; // 100

// +=, -=, *=, /=
health -= 20: health = health - 20

// ++, --
health ++; //81

// health = (health * 2) + (health / 5);